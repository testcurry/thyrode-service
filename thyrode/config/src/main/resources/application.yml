server:
  port: 10001

spring:
  cloud:
    config:
      server:
        git:
          uri: git@github.com:testcurry/spring-config.git
  profiles:
    active: dev, local, test

  security:
    user:
      name: curry
      password: 123456